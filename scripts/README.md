# Scripts

Restricted Lua activity definitions live under this boundary. They define objectives, triggers, and completion rewards. Reward identifiers and positive quantities are validated by the server-owned inventory domain; scripts cannot create arbitrary inventory items.
